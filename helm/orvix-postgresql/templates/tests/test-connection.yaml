apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "orvix-postgresql.fullname" . }}-test-connection"
  labels:
    {{- include "orvix-postgresql.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "orvix-postgresql.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
